import jupiter from "@/app/adapters/jupiter";
import { getWalletBalance } from "./getWalletBalance";

export async function getCombinationSwapRoutes(walletAddress: string, outputAmount: number, outputMint: string) {
}